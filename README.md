# Disaster-Dataset
A dataset with more than 9000 images across 6 classes collected 'manually', all classes are connected to disasters/calamaties and with this dataset, disaster management, identifier models can be trained.
